export type VertJsEnv = {
  SRC?: string;
  DIST?: string;
  STATIC?: string;
  HOST?: string;
  STATIC_HOST?: string;
  PORT?: string;
  HTTPS?: string;
  KEY?: string;
  CERT?: string;
  PASSPHRASE?: string;
};
