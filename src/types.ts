export type VertJsEnv = {
  SRC?: string;
  DIST?: string;
  STATIC?: string;
  HOST?: string;
  PORT?: string;
  STATIC_HOST?: string;
  STATIC_PORT?: string;
  HTTPS?: string;
  KEY?: string;
  CERT?: string;
  PASSPHRASE?: string;
};
